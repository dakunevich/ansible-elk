stream {
  upstream logstash_servers {
    server 192.168.0.251:5000;
    server 192.168.0.252:5000;
  }

  server {
    listen 5000 udp;
    proxy_pass logstash_servers;
    proxy_responses 1;
    error_log /var/log/nginx/logstash.log;
  }
}
